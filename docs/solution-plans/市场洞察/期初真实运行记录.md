# 市场洞察期初真实运行记录

更新时间：2026-06-16

## 当前数据状态

- 数据源：807 个未删除数据源，覆盖综合资讯、行业媒体、电商搜索、专利搜索、财经资讯、公众号、政府政策和企业官网等类型。
- 周期启用数据源：805 个；其中综合资讯 176 个、行业媒体 143 个、电商搜索 146 个、财经资讯 140 个、专利搜索 140 个、政府政策 30 个、公众号 24 个、企业官网 6 个。799 个搜索型周期源已配置 LLM 筛选提示，6 个官网/网页源走正文抓取 AI 分析。
- 已成功执行数据源：627 个（按成功任务中的 `data_source_id` 去重计）；搜索发现成功任务 809 个，正文抓取成功任务 1923 个。
- 可用正式情报：2206 条（按 `status='active'`、`review_status='approved'` 且未删除计），且摘要、情感、标签和来源 URL 均已覆盖。
- 公司隔离情报：1704 条；公共主题情报：319 条。
- 活跃候选：140 条。
- 报告：29 份，覆盖市场洞察周报、竞对动态、客户新品机会、研发课题趋势、政策法规、电商新品监测、深度研究等类型；已多轮生成健源专属、御馨专属、公共行业主题、AI 深化样本、电商新品监测培训报告。
- 报告模板：已上架 7 个市场模板，分别为市场洞察周报、竞对动态报告、客户新品机会报告、研发课题趋势报告、政策/法规简报、电商新品监测报告、深度研究报告。
- 报告素材：1505 条有效素材，已为培训账号沉淀“期初真实运行素材池”；已清理所有引用 inactive 或未审核通过情报的有效素材，当前有效素材坏引用为 0。
- 任务状态：历史中断残留的 `running` 任务已监督清理为失败状态并记录原因；本轮新增清理 2 条超过 30 分钟未完成任务，清理后超时未完成任务为 0；当前调度器按轻量搜索策略运行，质量运营页可直接展示等待任务、运行中任务和超时未完成任务。
- 调度器状态：本机 `.env` 已开启 `INSIGHT_SCHEDULER_ENABLED=true`；当前后端进程已通过平台接口启动调度器，状态为 `enabled=true`、`running=true`、配置健康 `ready`。
- 相关性复筛：已将 305 条低相关或无效占位正式情报降级为 `inactive/needs_review`，不再进入正式情报列表和报告素材。
- AI 深化：带“待AI深化”痕迹的轻发现情报已清零；当前 active/approved 情报全部已有摘要、情感、标签和来源 URL，并保留 AI 机会/风险等结构化字段，其中机会点覆盖 1870 条、风险点覆盖 1901 条。轻量搜索发现链路已改为平台侧当场调用 AI 初筛，候选入库时即写入摘要、标签、情感、机会点、风险点、相关性分和置信度。

## 已完成运行动作

- 已从需求调研、信息采集需求和网址整理文件导入并扩展真实数据源。
- 已按企业扩展综合动态、经营财经、专利技术、电商新品、行业媒体等搜索型数据源。
- 已调用平台接入的大模型生成摘要、标签、情感倾向、机会点、风险点和置信度。
- 已将高置信 AI 候选批量转为正式情报，并加入报告素材池。
- 已生成多类型报告草稿，报告引用真实情报素材。
- 已基于最新入库情报追加生成 5 份二次培训报告，其中健源、御馨专属报告按所属公司授权，公共行业、电商、AI 深化样本报告公共可见。
- 已继续执行 22 个数据源，补充综合动态类真实情报，并对新增搜索轻发现情报做 AI 摘要、标签、情感、机会点和风险点富化。
- 新增正式情报相关性复筛机制，调用平台 AI 判断是否属于食品饮料、功能糖/淀粉糖、植物蛋白、配料原料、客户/竞对、政策、专利、研发营销洞察范围；低相关内容自动降级为待复核。
- 已清理报告素材中引用低相关降级情报的 66 条素材，并重新生成 5 份最新质量口径培训报告。
- 已服务级抽测 AI 问情报和深度研究：围绕“低糖、功能糖、阿洛酮糖、饮料”问题检索到库内证据，AI 回答包含情报 ID，返回引用含来源 URL；深度研究生成 8 条证据矩阵。
- 已补充 AI 助手和深度研究生成模式标识：接口返回 `generation_mode`，前端展示“AI 生成 / 证据草稿 / 无证据”，AI 调用失败时不会伪装成 AI 结果，而是明确标识为基于库内证据的规则草稿。
- 已新增独立 AI 助手页面 `/insight/assistant`，并加入 Insight 侧边栏和移动导航；页面支持 AI 问情报、深度研究、所属公司/企业/数据源/课题/标签/情感/日期筛选、引用来源展示、证据矩阵展示，以及深度研究保存为报告草稿。
- 已优化 AI 助手首屏交互：模式选择从下拉框改为常驻双模式卡片，用户打开页面即可看到“AI 问情报”和“深度研究”，并能理解深度研究会检索证据、提炼机会/风险、保存为报告。
- 已补齐报告中心深链：`/insight/reports?report_id=<id>` 会直接加载对应报告详情；AI 助手深度研究保存报告后可一键跳转到生成的报告草稿。
- 已继续执行综合动态、行业媒体、专利等数据源，完成财经、综合动态、行业媒体、专利搜索轻发现情报的多批次 AI 富化和相关性复筛。
- 已扩展并保留 807 个未删除数据源，其中 805 个启用周期调度；周期源分布为综合资讯 176 个、行业媒体 143 个、电商搜索 146 个、财经资讯 140 个、专利搜索 140 个、政府政策 30 个、公众号 24 个、企业官网 6 个。
- 已将 700 个仍为手动模式的启用数据源纳入 daily 周期调度，并按未来 24 小时错峰设置 `next_run_time`；随后通过平台调度服务执行 1 次到期批次，检查 805 个周期源、20 个到期源，本轮执行 5 个数据源且全部成功。
- 已验证常驻调度器在当前后端进程中自行执行：启动后自动创建 `scheduler_tick` 任务，`triggered_by='scheduler'`，检查 805 个周期源、21 个到期源，本轮执行 5 个数据源且全部成功，结束后 `running` 任务数为 0。
- 修复周期调度 `crawl_top_n=0` 被误当作空值的问题：此前 `0 or max_results` 会导致调度器仍抓取正文并被慢 URL 阻塞；现已保留显式 0，周期调度专注“搜索发现 + 候选入库”，正文级/摘要级 AI 深化由独立批处理脚本执行。
- 已验证修复后调度扫描：检查 805 个周期源、19 个到期源，本轮执行 5 个电商新品数据源，32 秒完成且全部成功，每源发现 3 条并生成 3 条候选；最新搜索任务入参均为 `crawl_top_n=0`。
- 已补充周期调度单源超时保护，避免外部搜索或抓取异常时长期占用 `scheduler_tick`；最新验证批次检查 805 个周期源、15 个到期源，12 秒完成 5 个数据源，全部成功且无 `running` 残留。
- 已继续监督平台常驻调度自动运行，最新 `scheduler_tick` 任务 ID 为 3220，检查 805 个周期源、7 个到期源，执行 5 个电商新品数据源，全部成功且无失败。
- 已补齐业务报告模板市场：7 个模板均来自数据库 `insight_report_template`，前端报告中心不再依赖静态兜底模板。
- 已修正“轻量搜索发现只入搜索摘要、后续再 AI 深化”的缺口：搜索任务现在会先执行 LLM 结果筛选，再对保留搜索命中批量执行 AI 初筛；最新验证任务 `keyword_search_discovery` ID 3239 已记录 `llm_filter_configured=true`、`llm_filter_applied=true`、`hit_ai_analysis_applied=true`。
- 已补充质量运营和数据源页监督能力：质量运营 AI 指标现在展示 LLM 筛选任务、搜索 AI 初筛任务和搜索 AI 初筛候选；数据源测试结果和执行日志会展示“已配置 LLM 筛选 / LLM 已判分 / AI 初筛已入库”等状态，便于培训和现场排障。
- 已补强任务健康监督：质量运营采集质量指标新增等待任务数、运行中任务数和超过 30 分钟未完成任务数；本轮通过项目内置 `cleanup_stale_tasks` 服务清理任务 3226、3229，清理后超时未完成任务为 0。
- 已修正搜索筛选空结果的任务语义：搜索通道可用但命中结果被规则或 LLM 全部过滤时，任务记为成功且候选数为 0，并保留 `filter_summary`、`rejected_items` 和 LLM 判分信息；只有通道调用失败或未配置通道才标记失败，避免正常筛选噪声污染调度失败率。
- 已按新语义重分类历史空结果任务：5 条 `keyword_search_discovery` 空结果失败任务和对应 5 条 `scheduler_tick` 任务已改为成功，`output_payload` 记录 `empty_result_reclassified`，当前完整验收中“搜索空结果不计为失败任务”为 0 个失败。
- 已增强搜索采集容错：当搜索命中后的 Firecrawl 正文抓取超时或失败时，父级搜索任务不再丢失该命中，会基于搜索标题、摘要和 URL 生成“搜索命中候选”，并写入“正文抓取待补”标签和 `crawl_errors`。已用数据源 981 `莫林/达芬奇-电商新品` 试跑验证：Firecrawl 返回 408，但搜索任务 3299 成功保留 1 条候选。
- 已补充情报中心批量运营入口：候选情报支持本页勾选后批量通过、批量忽略、批量拒绝；正式情报支持批量加入报告素材池、批量标记重点、批量隐藏，前端复用 `/api/v1/insight/intelligence/bulk-action`，不再需要逐条点击处理。
- 已补齐正式情报批量修正入口：本页勾选正式情报后可一次性修正标签、情感倾向和重要性；后端新增 `patch_metadata` 批量动作，留空字段不会覆盖原值，适合培训期对 AI 标签和情感做人工校准。
- 已补齐情报中心高级筛选入口：正式情报支持按关键词、主题类型、情报类型、所属公司、企业、情感、数据源、课题、标签和发布日期范围筛选，并且 AI 问情报/深度研究会继承当前筛选条件；候选审核支持关键词、主题类型、情报类型、数据源和候选来源筛选。
- 已补充数据源配置页批量运营入口：本页勾选后可批量启用、批量停用、批量设置每日周期、批量切换轻量 AI 模式、批量设置指定可见、批量执行轻量采集，前端复用 `/api/v1/insight/data-sources/bulk-action`，执行动作默认 `crawl_top_n=0`，避免误触发大规模正文抓取。
- 已补齐数据源导入结果预览：Excel/Word 导入成功后弹窗不再立即关闭，会展示解析、新增、更新、失败、暂未自动化渠道统计，以及导入明细、关键词、企业/课题、来源文件、失败原因和不可自动化渠道原因，便于用户现场确认后再关闭。
- 已补齐数据源导入前预览：新增 `/api/v1/insight/data-sources/import-preview`，前端导入弹窗支持“先预览”和“确认导入”两步；预览只解析文件、匹配企业并标记“将新增/将更新/将恢复”，不会写数据库。
- 已补充细粒度批量授权入口：数据源配置页和情报中心均可对本页勾选对象打开批量权限弹窗，一次性授权给全员、指定用户、角色或部门，并选择查看、编辑或所有者权限；批量授权复用 `/api/v1/insight/permissions/{target_type}/bulk`，不会绕过后端权限过滤。
- 已完成情报详情页显眼占位治理：右侧“相似情报推荐”不再展示“同主题最新正式情报待接入”等静态占位，改为复用正式情报列表接口按主题、类型和来源查询真实相似情报；无结果时展示真实空状态。已用浏览器验证可见情报 1268：页面显示真实推荐链接且不含“待接入”。
- 已清理 Insight 前端遗留演示组件和静态热点元数据：删除未使用的 `DataTableCard` 静态情报表，移除 `DemoPrimitives` 中未使用的固定折线图/环图函数；“今日热点”排行不再补固定公司名和固定浏览量，只展示接口返回的真实标题与排序。
- 已继续清理 Insight 前端残留演示组件：移除未使用的静态日期、固定建议列表、演示搜索/筛选控件、演示公司 Logo 和来源图标等公共导出，避免后续页面误用静态展示内容；当前保留的 `DemoPrimitives` 仅作为真实页面使用的基础卡片、标签、统计卡、排行和章节头。
- 已继续扫描 Insight 前端“待接入/示例/模拟/假数据”文案：当前剩余“待接入”主要集中在报告模板上传后的 DOCX/XLSX 套版能力说明，属于真实边界提示；企业微信“模拟发送”来自真实发送开关未启用时的受控状态说明，不作为假数据处理。后续仍需按页面继续做视觉和交互走查。
- 已补强数据源页错误解释与排障深链：数据源测试结果、执行日志、调度器状态和调度批次不再直接暴露 Firecrawl/HTTP 原始错误，统一展示业务可理解的失败原因、恢复建议和可展开技术详情；数据源页支持 `?data_source_id=<id>` 自动选中数据源并打开执行日志。已用浏览器验证 `data_source_id=981`：日志显示“正文抓取超时”、恢复建议和“技术详情”。
- 已补齐质量运营到数据源日志的下钻入口：质量运营页“数据源质量排行”支持点击数据源名称或“查看日志”跳转到 `/insight/data-sources?data_source_id=<id>`，自动定位对应数据源执行日志，便于监督平台真实跑数时快速介入失败源。
- 已新增平台真实运行监督脚本 `backend/scripts/insight_runtime_supervisor.py`，只读输出数据源覆盖、最近任务成功/失败、运行中任务、近期失败、正式情报/候选汇总、AI 字段覆盖和今日分时新增；统计窗口和运行中任务年龄使用上海时区当前时间与最新任务时间的稳妥锚点，既避免数据库时区偏移导致负年龄，又能让卡住任务年龄持续增长。最新完整验收显示：805 个周期数据源启用，运行中/等待任务为 2 个且均未超时，active/approved 正式情报 2206 条且均有摘要。
- 已补充 AI 字段覆盖监督：当前 active/approved 正式情报 2206 条，摘要、情感、标签和来源 URL 覆盖均为 2206 条；机会点覆盖 1870 条，风险点覆盖 1901 条，证明平台侧 AI 已参与筛选、摘要、标签、情感和机会/风险提取，且缺机会/风险的条目不是前端假补。
- 已补充报告素材健康监督与清理脚本：`backend/scripts/insight_clean_report_materials.py` 可重算报告有效素材数并清理正文 `evidence_ids` 中已软删或已降级情报；本轮已修正 13 份历史报告的素材计数和证据 ID，当前 `report_material_health` 显示有效素材坏引用 0、报告素材计数不一致 0、有效报告素材 1513 条。
- 已新增完整可用化验收脚本 `backend/scripts/insight_full_readiness_acceptance.py`，只读检查数据源规模、渠道类型、正式情报数量、AI 字段覆盖、任务堆积、搜索空结果任务语义、报告模板、报告生成数量、报告素材健康、报告正文降级情报残留、健源/御馨真实账号权限隔离、计划公开接口、AI 助手/深度研究入口、AI 助手/深度研究真实调用、暂未实现渠道清单和 Insight 前端静态演示数据残留。2026-06-16 最新执行结果为 26/26 通过：807 个数据源、805 个启用周期源、2206 条 active/approved 正式情报、1505 条有效报告素材、计划公开接口后端和前端客户端均接通，运行中/等待任务 2 个且 30 分钟以上遗留任务 0，搜索空结果失败任务 0，AI 问情报和深度研究均以 `llm` 模式返回真实引用，报告正文降级情报残留为 0，前端静态演示残留扫描为 0。
- 已对齐报告中心真实交付能力：后端模板 `export_formats` 统一返回当前实际可执行的 `html/pdf/docx`，前端模板市场和上传模板区明确提示“当前可导出 HTML/PDF/DOCX；XLSX 套版导出尚未接入”，避免把规划能力误当成已上线能力。
- 已补齐 PDF 通用报告导出：后端使用 `reportlab` 基于报告内容生成 PDF，写入 `insight_report_export` 并复用报告查看权限；前端报告详情新增“导出 PDF”按钮，导出历史可下载或按原格式重试。
- 已补齐 DOCX 通用报告导出：后端使用 `python-docx` 基于报告内容生成可编辑 Word 文件，包含摘要、章节、证据卡片和参考素材；前端报告详情新增“导出 DOCX”按钮，导出历史可下载或按原格式重试。已通过真实报告导出验证：报告 29 生成 `电商新品与渠道信号监测报告_v1_export_3.docx`，状态 success，文件大小 59,641 字节。
- 已补充报告生成模式透明度：报告生成时将 `generation_mode` 写入 `content_json`，前端生成成功提示和报告阅读页展示“AI 生成 / 证据草稿 / 库内证据”，避免 AI 失败后的规则兜底被误解为模型生成内容。
- 已优化报告中心培训体验：桌面端报告历史与报告正文从 `xl` 宽度起并排展示，报告正文区域可直接看到权限、企微推送、HTML/PDF/DOCX 导出和导出历史；“按数据源选材”默认折叠，避免 800+ 数据源列表遮挡报告阅读。
- 已补齐报告中心定时生成与企业微信推送计划：新增 `insight_report_subscription`，支持按报告模板、报告类型、素材池、所属公司全部企业、指定企业或指定数据源配置日报/周报/月报；到期执行按计划创建者身份重新校验模板、企业、数据源和报告生成权限，生成报告后复用 `insight_notification` 写企业微信推送记录。前端报告中心新增“定时报告”入口、计划列表、立即执行、启停和删除操作。
- 为期初批量运行补充搜索轻发现模式：当正文抓取或 AI 深化耗时较高时，可先保存真实搜索命中的标题、URL、摘要为候选，并标记“待AI深化”，后续继续分批做正文级 AI 分析。
- 为期初运行脚本补充单源子进程执行、进程级超时、渠道过滤、失败源跳过和搜索命中候选生成开关，避免单个反爬或慢源阻塞整批运行。
- 新增搜索轻发现情报深化脚本，优先按真实 URL 抓取正文并调用 AI 生成摘要、标签、情感、机会点和风险点；正文抓取失败时，退回到真实搜索摘要级 AI 富化，并在 `raw_payload.deepening_mode` 中明确标记，避免伪造正文。
- 首批已完成 21 条“待AI深化”情报的 AI 富化，失败标记 14 条，剩余 691 条可继续按渠道分批深化。

## 本轮修复

- 修复搜索结果标题、URL、摘要过长导致 `insight_crawl_result` 入库失败的问题，入库前按字段长度截断。
- 清理明显低质页面，包括百度安全验证、贴吧占位、图片搜索结果等。
- 修正未来发布时间，例如 2030、2040 等由页面噪声误解析的日期，统一置为发布时间未知。
- 补充企业情报回填，将可匹配企业名称/简称的正式情报绑定到企业档案。
- 支持 `sys_company` 权限主体，通过用户部门所属公司实现健源/御馨隔离。
- 本轮清洗后确认：错误页/验证码/无效标题为 0 条，未来发布时间为 0 条。
- 情报 `owner_user_id` 已清空，避免个人 owner 绕过公司隔离；有公司归属的数据按 `sys_company` 授权，无公司归属的行业/政策/主题情报保持公共可见。
- 深化脚本已内置 URL 排除规则，跳过百度图片、百度验证码、京东联盟跳转、美团验证、淘宝 SEO 跳转、百度爱采购、百科类页面等不适合正文抓取的来源。
- 正式情报列表和详情接口已补充 `status='active'` 过滤，确保被质量复筛降级的情报不会继续出现在正式列表和详情入口。
- 相关性复筛脚本已新增低质量来源硬规则：文档分享站、问卷站、登录墙摘要、二次上传文件标题等会直接降级为待复核。本轮抽样发现并降级 10 条低展示价值正式情报，包括原创力文档、豆丁文档、问卷星、登录墙摘要等；随后清理报告素材和正文残留，完整验收确认坏引用为 0。
- 期初运行脚本目标计数已改为 active 正式情报口径，避免把低相关降级数据计入 2000 条验收目标。
- 前端首页、报告中心和企业档案已移除静态数据兜底：报告模板完全来自后端接口，趋势图、来源分布和类型分布无真实数据时展示空状态，不再用假数组撑 UI。
- 前端数据源配置页的统计卡片已改为“当前页”口径，避免把接口分页采样误读为全量 805 个数据源；全量运行口径以调度器、质量运营和数据库验收为准。
- 前端情报中心已加入本页选择和批量操作条，解决候选审核、素材池维护和重点情报标记需要逐条操作的问题。
- 前端情报中心已加入批量修正弹窗，解决 AI 生成标签、情感和重要性需要逐条打开详情修改的问题；后端会按当前用户权限逐条校验后再写入。
- 前端情报中心已补齐后端已有筛选条件，用户可以直接按所属公司、企业、情感、标签、课题、数据源和日期范围定位情报；AI 助手和深度研究沿用这些条件检索库内证据。
- 前端数据源配置页已加入本页选择和批量操作条，解决数据源启停、周期、轻量 AI 配置、可见范围和轻量执行需要逐条处理的问题。
- 前端数据源配置页已升级为采集计划优先的聚合管理：默认按采集类型/采集计划折叠展示，例如综合动态、行业媒体、电商新品、经营财经、专利技术、政策监管、公众号等，当前普通用户可见数据源可折叠为 8 个计划组；无关联企业的数据源不再堆成一个“未关联企业”杂项，而是进入对应采集计划并显示未关联数量。企业分组保留为第二层下钻，明细列表保留为排障视图，避免 800+ 底层数据源直接堆给用户管理。
- 前端数据源配置页已补齐计划组内下钻：计划组第一层只展示计划名称、覆盖企业数、未关联主题源数、数据源数、周期数、下次执行和异常状态；点击“展开”后在当前页展开到企业/主题子组，子组可单独选择、授权和轻量执行；点击“明细”可直接切到该采集类型的底层数据源列表。
- 前端数据源配置页已补齐组级可编辑能力：采集计划组和企业组均提供“配置”弹窗，可一次性修改组内真实数据源的抓取周期、权限范围、轻量 AI、自动审核、审核置信度和报告素材池策略；组级“启用、每日周期、权限、轻量执行”也可直接作用于本组，不再要求先选择本组再操作。
- 前端数据源配置页已新增“批量配置采集计划”弹窗：用户一次选择多个企业和多个采集类型，统一配置周期、AI 筛选、自动审核、报告素材池、权限范围和是否更新已有数据源；后端通过 `/api/v1/insight/data-sources/batch-create` 生成或更新稳定编码的数据源，日常仍按企业组聚合展示。
- 前端数据源导入弹窗已从“完成后关闭”改为“完成后保留预览”，用户可以在弹窗中检查生成数据源、失败条目和暂未自动化渠道，减少重复导入和漏看失败原因。
- 前端数据源导入已改为先预览再确认导入，用户可以在写库前看到生成明细、企业/课题匹配、关键词和暂未自动化渠道，减少批量误导入风险。
- 前端权限配置弹窗已接入批量目标：数据源和正式情报都可批量添加同一条授权规则，解决部门/角色/用户授权需要逐条打开对象的问题。
- 后端情报和报告接口已补充公司边界过滤：非管理员访问企业绑定情报、报告和报告选材时，除公开无公司主题内容外，必须匹配当前用户部门归属的 `sys_company`；`public` 或 `all` 授权不再放大到跨所属公司内容，详情直连和报告生成选材同样受限。
- 后端数据源接口已补充同样的所属公司硬隔离：非管理员访问、详情、编辑、执行和批量配置数据源时，除未关联企业的公共主题外，企业绑定数据源必须匹配当前用户部门归属的 `sys_company`；`public` 或 `all` 授权不再放大到跨所属公司数据源，普通用户也不能为其他所属公司的企业批量创建数据源。
- 报告详情权限已从误用“编辑权限”修正为“查看权限”：健源/御馨用户能打开本公司授权报告详情并看到真实素材引用，仍不能打开对方公司报告详情；更新、导出等写操作继续走独立权限校验。
- 报告素材清理脚本已升级为“素材引用 + 正文残留”双清理：除软删无效素材和重算素材数外，还会递归清理已降级情报标题/摘要在历史报告正文、风险、机会等字段中的残留。本轮清理后，报告 26 中曾残留的油井专利片段已移除，完整验收确认报告正文降级情报残留为 0。
- AI 助手证据检索已补充同样的公司边界过滤：非管理员即使指定其他 `sys_company_id` 检索，也不会因公开或全员授权读到其他所属公司的企业绑定情报。
- 完整验收脚本已从“检查 AI 助手页面和接口存在”升级为“实际调用 AI 问情报和深度研究”：管理员围绕低糖/功能糖检索健源情报，问答返回 6 条引用，深度研究返回 8 条证据矩阵，生成模式均为 `llm`；御馨用户跨查健源返回无证据。
- 运行监督脚本已修复 `timedelta` JSON 序列化和任务年龄时区口径，运行中任务年龄改为基于最新任务时间计算，避免出现负年龄或误判超时。
- 运行监督脚本和完整验收脚本已进一步修正任务年龄锚点：改为使用上海时区当前时间与最新任务时间的较大值，避免在无新任务时运行中任务年龄停止增长。本轮发现并通过正式 `cleanup_stale_tasks` 服务清理任务 3347、3348，清理后运行中/等待任务 2 个、30 分钟以上遗留任务 0 个。
- 统一模型工厂已将 `extra_body` 改为 `ChatOpenAI` 显式参数，保留思考模式开关，同时消除 AI 助手和深度研究真实调用时的 LangChain 参数警告。
- 已执行前端生产构建验证：`frontend` 目录执行 `pnpm build` 成功，`tsc -b` 与 `vite build` 均通过，确认当前 Insight 页面、AI 助手、报告中心、批量操作和导入弹窗相关 TypeScript 代码可进入生产包。
- 前端报告模板市场已区分“真实导出”和“待接入套版能力”；上传 Word/Excel 模板仅用于解析章节、表格、字段和 Prompt，不再在交互上暗示可直接按原文件套版导出。
- 已将 151 条历史中断残留的 `running` 采集任务标记为 `failed`，错误信息写明“期初运行监督清理：未发现对应采集进程”，便于质量运营页面真实反映平台运行结果。
- 已将本轮新增的 2 条超时 running 任务通过正式服务清理为 failed，并写入 `output_payload.cleanup`；清理后质量运营和数据库复核均显示超时未完成任务为 0。

## 权限验证

- 健源用户示例：用户 40，只能看到 97 个健源企业，御馨企业 0 个。
- 御馨用户示例：用户 720，只能看到 43 个御馨企业，健源企业 0 个。
- 管理员示例：用户 5337，可看到全部 140 个企业。
- 情报可见性示例：用户 40 按 `sys_company_id=22` 查询御馨情报返回 0 条；用户 720 按 `sys_company_id=6` 查询健源情报返回 0 条；管理员 5337 可看到健源 1597 条、御馨 251 条。正式情报按后端权限、所属公司边界和 active 状态过滤；低相关降级内容不进入正式列表。
- 报告可见性示例：用户 40 可见健源专属报告 25，不可见御馨专属报告 26；用户 720 可见御馨专属报告 26，不可见健源专属报告 25；管理员 5337 可同时看到报告 25 和 26。
- AI 助手验证示例：管理员账号围绕低糖和功能糖提问时，返回 6 条证据引用且含来源 URL；深度研究返回 8 条证据矩阵且含来源 URL，生成模式均为 `llm`。御馨用户指定健源所属公司检索时返回无证据。回答基于 `insight_intelligence.status='active'` 且 `review_status='approved'` 的库内证据。
- 质量运营验证示例：AI 质量指标已能从真实任务和候选中计算出 LLM 筛选任务 54 个、搜索 AI 初筛任务 12 个、搜索 AI 初筛候选 41 条；采集质量指标已展示等待任务 0、运行中任务 2、超时未完成 0。
- 最新完整验收快照：2026-06-16 执行 `uv run python scripts/insight_full_readiness_acceptance.py`，结果 26/26 通过；active/approved 正式情报 2206 条、未删除数据源 807 个、启用数据源 805 个、周期调度数据源 805 个、有效报告素材 1505 条、计划公开接口后端和前端客户端均接通、运行中或等待任务 2 个、30 分钟以上遗留任务 0 个，搜索空结果失败任务 0 个，健源/御馨真实账号权限隔离通过，报告详情本公司可读且跨公司详情阻断，报告正文降级情报残留 0 个，AI 助手与深度研究真实检索调用通过，Insight 前端静态演示残留 0 个。
- 最新前端构建快照：2026-06-16 在 `frontend` 执行 `pnpm build`，结果通过；产物写入 `frontend/dist`，未出现 TypeScript 或 Vite 构建错误。

## 当前差距

- 正式情报不少于 2000 条的期初目标按 active/approved 口径已达成，当前为 2206 条；另有低相关、低质量来源或无效占位内容已转待复核，不作为可用正式情报。
- “搜索轻发现”情报的待深化积压已清零；后续重点转为继续扩大相关性复筛覆盖面、建设电商商品结构化采集，而不是全部用网页正文抓取。
- 报告中心当前已支持库内素材生成、Word 式阅读、人工编辑版本、HTML/PDF/DOCX 通用导出和导出历史下载；DOCX 官方模板套版与 XLSX 套版导出仍未接入，已在前后端真实能力字段和页面提示中标明，后续需要补模板渲染器或接入文档转换服务。
- 部分电商和官网页面存在反爬、超时或跳转链接过长问题，已通过搜索型数据源和字段截断缓解；强登录、验证码、付费内容仍按暂未实现渠道清单处理。
