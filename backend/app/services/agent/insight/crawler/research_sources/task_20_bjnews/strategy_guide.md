# 新京报爬虫安全与防封策略

- **直连基站检索专区**：访问官方专门搜索入口 `bjnews.com.cn/search?bwsk=...`，保障原厂新闻直连的稳定性与及时性。
- **精确文本切分**：对 `div.bom` 的字符串利用 `re.search(r'\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}')` 定位时间标志，切分出前半部为署名作者（如千龙网、新京报），后半部为时间。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
