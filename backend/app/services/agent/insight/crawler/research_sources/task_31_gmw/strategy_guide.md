# 光明网爬虫安全与防封策略

- **站内搜索直连**：直连 `https://zhonghua.gmw.cn/news.htm?q=...`，避开主页复杂的搜索框结构。
- **高精度卡片解析**：从 `div.m-news-box` 节点中精准提取标题、最终超链接、摘要、时间与发布来源。
- **原始文章超链接获取**：优先使用 `p.u-links` 所展示的移动版/电脑版最终地址，而非系统内部查询的 `.do` 网址。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
