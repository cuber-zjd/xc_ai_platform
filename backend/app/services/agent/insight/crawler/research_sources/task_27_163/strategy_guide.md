# 网易新闻爬虫安全与防封策略

- **站内搜索子域直连**：直连 `www.163.com/search?keyword=...` 发送请求，绕过主页面加载开销。
- **高精度卡片解析**：从 `div.keyword_new` 节点中提取标题、链接与作者。
- **秒级时间抓取**：提取 `div.keyword_time`，补全为标准的 `YYYY-MM-DD 00:00:00` 格式，规避正则转换的出错率。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
