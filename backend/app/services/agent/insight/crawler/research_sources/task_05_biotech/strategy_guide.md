# 生物通爬虫安全与防封策略

- **Bing 站内代理检索**：直接访问 `cn.bing.com`，避开生物通主站 404 故障和 WAF 封锁。
- **域名强校验过滤**：由于必应在无结果时会降级吐出百科或官网链接，程序在抽取时对 URL 进行 `if "ebiotrade.com" not in link:` 过滤，确保返回数据纯净度 100%。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
