# 人民网爬虫安全与防封策略

- **站内搜索子域直连**：直连 `search.people.cn/s?keyword=...` 发送请求，绕过主页无搜索框的限制。
- **高精度卡片解析**：从 `div.content` 节点中提取标题、摘要和渠道来源。
- **秒级时间抓取**：提取 `span.tip-pubtime`，本身就是标准的 `YYYY-MM-DD HH:MM:SS` 格式，规避正则转换的出错率。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
