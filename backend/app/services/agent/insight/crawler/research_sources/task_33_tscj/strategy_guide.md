# 泰山财经爬虫安全与防封策略

- **站内搜索直连**：直连 `http://so.sdnews.com.cn/cse/search?q=...`，避开主页及财经二级域名中静态摆设的输入框。
- **高精度卡片解析**：从百度站内搜索卡片 `div.result` 中，洗涤掉 `<em>` 等高亮多余标签，高保真提取标题、最终超链接、摘要内容。
- **时间正则清洗**：从卡片底部 `span.c-showurl` 中，用正则捕获文章发布日期并转换为标准 `YYYY-MM-DD 00:00:00`。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
