# 新华网爬虫安全与防封策略

- **官方搜索中心基站直连**：直接访问 `so.news.cn`，规避多余广告及非检索路由载入开销。
- **无感回车与延时交互**：向 `input.input` 输入文字后，使用 Playwright 自带的 `press("Enter")` 触发检索，提供 6 秒挂起缓冲。
- **秒级时间抓取**：提取 `div.pub-tim`，本身就是标准的 `YYYY-MM-DD HH:MM:SS` 格式，规避正则转换的出错率。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
