# 今日头条爬虫安全与防封策略

- **绕过 A-Bogus 动态加密签名**：基于 Playwright 忽略后台接口签名校验，完全由浏览器渲染 DOM 并在页面挂载完成后提取数据。
- **跳转链接二次解码**：头条搜索的结果超链接通常被封装过，程序使用 `urllib.parse` 从 `url` 参数中分离出真正的目标网站真实链接。
- **资讯过滤**：限定参数 `pd=information` 强制避开视频和微头条的干扰。
- **多词冷却**：两次搜索切换时挂起 10 秒。
