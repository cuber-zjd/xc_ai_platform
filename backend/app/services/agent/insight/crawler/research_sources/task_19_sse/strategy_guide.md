# 上海证券交易所爬虫安全与防封策略

- **精确输入框交互**：锁定公告查询关键字输入框 `input[id='inputCode']` 进行模拟输入，点击 `button[id='btnQuery']` 触发。
- **6位证券代码正则强校验**：上交所公告列表使用 table 表格结构。脚本遍历 `tr` 时，强制首列满足 `re.match(r'^\d{6}$', code)`，这能彻底剔除非公告行的干扰，使得蜜雪冰城和茶百道（无公告）的空列表 JSON 安全落盘。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
