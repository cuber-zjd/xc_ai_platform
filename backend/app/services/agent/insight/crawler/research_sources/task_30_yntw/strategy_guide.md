# 云糖网爬虫安全与防封策略

- **站内搜索直连**：直连 `https://www.yntw.com/?s=...`，绕过需要点击展开的搜索框交互。
- **高精度卡片解析**：从 `li.item` 节点中提取标题、链接、摘要、时间与频道分类。
- **秒级时间抓取**：提取 `span.item-meta-li.date`，补全为标准 `YYYY-MM-DD 00:00:00` 格式。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
