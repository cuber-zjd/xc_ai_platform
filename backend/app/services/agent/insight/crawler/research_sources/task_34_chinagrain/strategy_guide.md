# 粮信网爬虫安全与防封策略

- **站内搜索直连**：直连 `https://www.chinagrain.cn/news/?param=hyxx&key=...`，避开主页复杂的表单交互。
- **高精度卡片解析**：从 `ul#list` 中的 `li` 节点中，精准过滤广告和无关栏目，仅提取正规新闻的标题、原始详情超链接。
- **标题日期智能抽离**：根据粮信网独特的把发布日期写在标题最前端的模式，使用正则 `^(\d{4})年(\d{1,2})月(\d{1,2})日` 精准捕获日期并转换为标准秒级时间。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
