# 食业头条爬虫安全与防封策略

- **客户端签名唤醒交互**：通过 `mobileSearch()` 调用直接利用了主页面已经建立的会话与 Token，规避了由于时间戳及签名过期报错的问题。
- **全 searchText 输入框赋值**：同时为多处 input.searchText 填入检索词并派发原生事件，确保状态绑定的完整覆盖。
- **中文字符相对时间还原**：将 `3周前`、`03月21日` 等多样化中文字符高精度还原为标准的 `YYYY-MM-DD HH:MM:SS` 规范。
- **多词冷却**：两次检索切换时强制挂起 10 秒。
